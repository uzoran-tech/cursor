name: Deploy to GitHub Pages

on:
  push:
    branches: [main, claude/flipboard-app-wpdet1]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm ci --prefix bloodtrack
      - run: npm run build --prefix bloodtrack
      - run: |
          mkdir -p dist/blood
          cp -r bloodtrack/dist/. dist/blood/
          touch dist/.nojekyll
      - name: Publish to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
